export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME ?? "Inertia";

export const APP_DESCRIPTION = "The financial tool that adapts to you";

export const LOCALES = ["en", "sr"] as const;
export const DEFAULT_LOCALE = "en" as const;
